{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Jugador</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Id_persona</th>
                <td>{{ entity.idpersona }}</td>
            </tr>
            <tr>
                <th>Id_club</th>
                <td>{{ entity.idclub }}</td>
            </tr>
            <tr>
                <th>Matricula_aag</th>
                <td>{{ entity.matriculaaag }}</td>
            </tr>
            <tr>
                <th>Handicap_par3</th>
                <td>{{ entity.handicappar3 }}</td>
            </tr>
            <tr>
                <th>Handicap_estandar</th>
                <td>{{ entity.handicapestandar }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_jugador') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_jugador_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
