{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar Formato de Juego</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_formatojuego') }}">
            Volver
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
