{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tarjeta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Id_jugador</th>
                <th>Id_torneo_fecha</th>
                <th>Posicion</th>
                <th>&nbsp;</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_tarjeta_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idjugador }}</td>
                <td>{{ entity.idtorneofecha }}</td>
                <td>{{ entity.posicion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_tarjeta_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_tarjeta_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_tarjeta_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
