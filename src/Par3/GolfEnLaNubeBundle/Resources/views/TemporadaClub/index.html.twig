{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TemporadaClub list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Id_temporada</th>
                <th>Id_club</th>
                <th>Capitanes</th>
                <th>&nbsp;</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_temporadaclub_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idtemporada }}</td>
                <td>{{ entity.idclub }}</td>
                <td>{{ entity.capitanes }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_temporadaclub_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_temporadaclub_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_temporadaclub_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
