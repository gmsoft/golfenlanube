{{ form_start(form, {'style': 'horizontal'}) }}

	{{ form_errors(form) }}
	{{ bootstrap_set_label_col(2) }}
	{{ bootstrap_set_widget_col(10) }}  

	{#{ form_row(form.nombre)  }#}
	{{ form_row(form.temporada_club)  }}
	
	<ul id='lista_jugadores' class="list-group">
	</ul>
	
	{{ form_widget(form.submit, {attr: {'class': 'disabled'}}) }}
	{#{ form_widget(form.club)  }#}
	{{ form_widget(form.torneo_fecha)  }}
	<!-- hidden -->
	{{ form_widget(form._token) }}
</form>
